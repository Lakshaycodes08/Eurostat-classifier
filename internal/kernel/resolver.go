// resolver.go will resolve a tool ID (e.g. stripe.createCustomer) to a Wrekenfile entry and tooling.json metadata.
package kernel

// This file will contain logic to resolve a tool string like
// "stripe.createCustomer" into a concrete Wrekenfile entry plus any
// additional metadata from tooling.json (e.g. allowed operations,
// output shape guarantees).
//
// For now it is left as a placeholder so the structure of the kernel
// package is clear to implementers.

